import
inport

export class CommandParser {
  constructor({ command, args }) {
    this.command = command
    this.args = args
  }
  
  toParsedCommand() {
    let ParsedCommand;
    
    return ParsedCommand
  }
}
